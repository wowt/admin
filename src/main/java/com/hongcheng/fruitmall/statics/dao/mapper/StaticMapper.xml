<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hongcheng.fruitmall.statics.dao.mapper.StaticMapper">
    <sql id="filter">
        <if test="qo.name != null">
            AND `productName` like CONCAT('%',#{qo.name,jdbcType=VARCHAR},'%')
        </if>
        <if test="qo.type != null">
            AND `productType`=#{qo.type,jdbcType=INTEGER}
        </if>
        <if test="qo.startTime != null">
            AND `createTime`&gt;=#{qo.startTime,jdbcType=TIMESTAMP}
        </if>
        <if test="qo.endTime != null">
            AND `createTime&lt;=#{qo.endTime,jdbcType=TIMESTAMP}
        </if>
    </sql>

    <select id="getStaticByQuery" resultType="com.hongcheng.fruitmall.statics.pojo.vo.StaticVO">
        select productId,productTitle,SUM(`quantity`) as totalCapacity,SUM(`order_product_relation.price`) as totalMoney
        from `order` inner join `order_product_relation` on `order`.`orderId`=`order_product_relation`.`orderId`
        inner join `product` on `order_product_relation`.productId=`product`.productId
        <where>
            <include refid="filter"/>
        </where>
        group by productId
        <choose>
            <when test="qo.orderBy != null">
                ORDER BY
                <foreach collection="qo.orderBy" item="item" separator=",">
                    ${item.field} ${item.dir}
                </foreach>
            </when>
            <otherwise>
                ORDER BY totalMoney desc
            </otherwise>
        </choose>
        <if test="qo.size != 0">
            LIMIT #{qo.from,jdbcType=INTEGER},#{qo.size,jdbcType=INTEGER}
        </if>
    </select>
    <select id="countStatic" resultType="java.lang.Integer">
        select COUNT(`productId`)
        from `order` inner join `order_product_relation` on `order`.`orderId`=`order_product_relation`.`orderId`
        inner join `product` on `order_product_relation`.productId=`product`.productId
        <where>
            <include refid="filter"/>
        </where>
        group by productId
    </select>
</mapper>